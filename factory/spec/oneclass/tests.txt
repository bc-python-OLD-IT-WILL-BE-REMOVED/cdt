/* -------------------------- *
 * -- BLOCKS FOR THE TESTS -- *
 * -------------------------- */

// See ``settings.txt`` for infos about the values used in this peuf file.

peuf::
    keyval::
        seps : =
        names: lesson
               home
               test
               info
               mcq


check::
    lesson-home-test-info-mcq::
        date  = :date:
        time  = :time:
        ref   = :refs_toc:
        title
